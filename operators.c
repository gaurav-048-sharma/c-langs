#include<stdio.h>
int main() {
    int x=10, y=5, sum ,sub,  mul, div;
    sum = x+y;
    printf("Sum of x and y is: %d\n", sum);
    sub = x-y;
    printf("Substraction of x and y is: %d\n", sub);
    mul = x*y;
    printf("multiply of x and y is: %d\n", mul);
    div = x/y;
    printf("division of x and y is: %d\n", div);
}